find_all_sources(. rendering_SRCS)
if(TARGET_OSX OR TARGET_LINUX)
  find_all_sources(gl system_SRCS)
elseif(TARGET_ATV2)
  find_all_sources(gles system_SRCS)
elseif(TARGET_WIN32)
  find_all_sources(dx system_SRCS)
endif()
add_sources(${rendering_SRCS} ${system_SRCS})
